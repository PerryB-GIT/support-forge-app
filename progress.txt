# Progress Log: Support Forge Visual Improvements

This file contains learnings and notes from each Ralph iteration.
Each Claude instance appends what it learned for future iterations.

---

## Iteration 1 - US-001

**Completed:** Added CSS animation utilities and hover effects to globals.css

**Learnings:**
- Project uses Tailwind CSS v4 with @import "tailwindcss" syntax
- CSS custom properties already defined for brand colors (--accent-glow, --forge-purple, etc.)
- No dedicated typecheck script - use `npx tsc --noEmit` in apps/web directory
- Added prefers-reduced-motion support for accessibility

**CSS Classes Added:**
- `.card-hover` - lift + glow effect on hover
- `.glow-purple` - purple glow on hover
- `.scale-hover` / `.scale-hover-subtle` - scale effects
- `.animate-fade-up` / `.animate-fade-up-stagger` - scroll reveal
- `.animate-pulse-glow` - pulsing glow effect
- `.animate-float` - floating animation

**Files changed:**
- apps/web/src/app/globals.css

---

## Iteration 2 - US-002

**Completed:** Added gradient background system to globals.css

**Learnings:**
- CSS pseudo-elements (::before, ::after) work well for overlay effects
- SVG data URIs inline in CSS work for noise textures (keep opacity very low ~0.03)
- Need `pointer-events: none` on overlay pseudo-elements to allow clicks through
- Grid patterns use repeating linear-gradient with background-size for tiling
- Glow orbs use radial-gradient with blur filter for soft light effect

**CSS Classes Added:**
- `.hero-gradient` - main hero linear-gradient with purple spotlight ::before
- `.hero-spotlight-right` - additional right-side radial gradient ::after
- `.section-gradient` - vertical gradient for content sections
- `.surface-gradient` - diagonal gradient for cards/elevated areas
- `.noise-texture` - subtle grain effect using SVG feTurbulence
- `.grid-pattern` - tech aesthetic grid overlay
- `.glow-orb` / `.glow-orb-small/medium/large` - decorative blur effects

**Files changed:**
- apps/web/src/app/globals.css

---

## Iteration 3 - US-003

**Completed:** Created HeroSection component with image support

**Learnings:**
- Next.js Image component with `fill` prop and `sizes` attribute handles responsive images
- Use `sizes="(max-width: 768px) 800px, (max-width: 1200px) 1200px, 1920px"` for breakpoints
- Layer overlays with z-index: image(0) < spotlights(1) < noise(2) < content(10)
- Inline CSS for radial gradients when classes aren't flexible enough
- Use animation-delay via inline style for staggered animations
- Components can use CSS classes from globals.css directly

**Props created:**
- `title` - main heading (required)
- `subtitle` - description text (optional)
- `backgroundImage` - URL for hero image (optional)
- `gradient` - enable gradient/spotlight effects (default: true)
- `children` - CTA buttons or additional content (optional)
- `size` - "sm" | "md" | "lg" for padding (default: "md")
- `align` - "left" | "center" for text alignment (default: "center")

**Files created:**
- apps/web/src/components/ui/HeroSection.tsx

---

## Iteration 4 - US-004

**Completed:** Added hero section to Homepage with floating particles

**Learnings:**
- HeroSection title prop should be ReactNode (not string) to support JSX with styled spans
- Floating particle system uses multiple animation keyframes with varied timing for organic feel
- Particles distributed via array math: `left: ${(i * 8.5) % 100}%` for even spread
- Connection lines add network/AI feel using gradient lines with pulse animation
- Wrap HeroSection in outer section for proper positioning of absolute particles container
- Always update prefers-reduced-motion when adding new animations

**CSS Classes Added:**
- `.particles-container` - absolute positioned container for particle system
- `.particle` / `.particle-sm/md/lg` - individual particle nodes with glow
- `.animate-particle-1/2/3` - varied floating animations (8s, 10s, 12s)
- `.particle-line` / `.animate-line-pulse` - connection lines between particles

**Files created:**
- apps/web/src/components/ui/FloatingParticles.tsx

**Files changed:**
- apps/web/src/app/globals.css (particle system CSS)
- apps/web/src/app/page.tsx (HeroSection integration)
- apps/web/src/components/ui/HeroSection.tsx (title prop type fix)

---

## Iteration 5 - US-005

**Completed:** Updated service cards with hover animations

**Learnings:**
- CSS animated gradient borders use mask-composite technique with `::before` pseudo-element
- Key properties: `padding: 1px` on pseudo-element creates border width
- `-webkit-mask` with `linear-gradient(#fff 0 0)` content-box and second gradient creates border mask
- `mask-composite: exclude` (or `-webkit-mask-composite: xor`) reveals only the border area
- `background-size: 300% 300%` with `background-position` animation creates flowing gradient effect
- Applied to 15 cards total across Challenge, Approach, Service pricing, and Results sections
- Existing card styles need `position: relative` for pseudo-element positioning (already in .service-card)

**CSS Techniques Used:**
- Gradient border via mask-composite (avoids border-image limitations)
- Animation on hover only (opacity 0 → 1 + background-position animation)
- Reduced motion support already added in US-001

**Files changed:**
- apps/web/src/app/globals.css (added .service-card class with animated border)
- apps/web/src/app/page.tsx (applied service-card class to 15 cards)

---

## Iteration 6 - US-006

**Completed:** Added scroll-triggered fade animations

**Learnings:**
- Intersection Observer API is perfect for scroll-triggered animations
- Create generic ref-returning hook for flexibility (`useScrollReveal<T extends HTMLElement>`)
- Stagger list animations with setTimeout array and clear on unmount
- `prefersReducedMotion` check should skip animations entirely, not just adjust timing
- Wrapper components (ScrollReveal, ScrollRevealList) simplify usage over direct hook calls
- `triggerOnce: true` with `observer.unobserve()` prevents repeated animations

**Hooks Created:**
- `useScrollReveal` - single element visibility detection with Intersection Observer
- `useScrollRevealList` - extends useScrollReveal with staggered visibleItems array

**Components Created:**
- `ScrollReveal` - wrapper with fade-up/fade-in/slide-up animations
- `ScrollRevealList` - wrapper for staggered list item reveals

**CSS Technique:**
- Tailwind transitions: `transition-all duration-500 ease-out`
- Animation classes: `translate-y-5 opacity-0` → `translate-y-0 opacity-100`
- Delay via inline style: `transitionDelay: isVisible ? \`${delay}ms\` : "0ms"`

**Files created:**
- apps/web/src/hooks/useScrollReveal.ts
- apps/web/src/hooks/index.ts
- apps/web/src/components/ui/ScrollReveal.tsx

**Files changed:**
- apps/web/src/app/page.tsx (applied ScrollReveal to all 5 homepage sections)

---

## Iteration 7 - US-007

**Completed:** Created custom icon components for Homepage pain points

**Learnings:**
- SVG icons should use `currentColor` for fill/stroke to inherit text color and enable theming
- Use `fillOpacity` (0.1-0.3) to create depth layers without hardcoding colors
- `strokeOpacity` (0.5-0.7) works well for secondary/decorative elements
- `strokeDasharray` creates dashed effects for uncertainty/motion (e.g., "4 3" or "6 2")
- Text elements in SVG use `textAnchor="middle"` for centering, need x/y positioning
- Use `<g transform="translate(x, y)">` to position groups of related elements
- Icon size prop with fixed options (24 | 48) keeps consistent usage across app

**Icon Design Patterns:**
- Racing rockets: Lead rocket larger/brighter, trailing smaller/faded, speed lines
- Chart uncertainty: Bar chart with dashed trend line, question mark in circle overlay
- Boardroom: Presentation screen with stand, data visualization, speech bubbles
- Broken gear: Dashed stroke circle, crack lines, floating pieces with directional arrows

**Files created:**
- apps/web/src/components/icons/index.ts (barrel export)
- apps/web/src/components/icons/IconCompetitivePressure.tsx
- apps/web/src/components/icons/IconROIUncertainty.tsx
- apps/web/src/components/icons/IconBoardQuestions.tsx
- apps/web/src/components/icons/IconFailedExperiments.tsx

---

## Iteration 8 - US-008

**Completed:** Created custom icon components for Homepage benefits

**Learnings:**
- Brain icon: Split into left/right hemispheres with center connections for symmetry
- Rocket with clock: Overlay smaller element (clock) in corner to show dual concepts
- Shield icon: Good container shape for nesting other symbols (AI chip)
- Podium icon: Use varying heights and opacity to show rank hierarchy
- Star/sparkle effects: Use small path shapes with reduced opacity for celebration feel
- Text elements in SVG: Keep font sizes small (6-8px) for legibility at 48px render size

**Icon Design Patterns:**
- Strategic clarity: Brain with radiating beams, center glow point
- Speed to value: Rocket with clock in corner, speed lines on opposite side
- Risk mitigation: Shield outline with nested AI chip (pins extend to shield edges)
- Competitive edge: First-place podium with flag and sparkle decorations

**Files created:**
- apps/web/src/components/icons/IconStrategicClarity.tsx
- apps/web/src/components/icons/IconSpeedToValue.tsx
- apps/web/src/components/icons/IconRiskMitigation.tsx
- apps/web/src/components/icons/IconCompetitiveEdge.tsx

**Files changed:**
- apps/web/src/components/icons/index.ts (added benefit icon exports)

---

